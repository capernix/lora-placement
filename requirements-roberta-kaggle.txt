# Use Kaggle's CUDA-enabled PyTorch, or install the matching official wheel.
# A CUDA-specific torch wheel is intentionally not pinned here.
transformers==5.16.1
peft==0.20.0
datasets==5.0.1
tokenizers==0.23.2
accelerate==1.14.0
